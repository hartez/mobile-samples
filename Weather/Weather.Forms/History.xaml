<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Weather.Forms.History">
    <StackLayout Margin="10,80,10,0">
        <Label Text="Forms!"></Label>
        <Label Text="Forms!" TextColor="BlueViolet" ></Label>
        <Button Text="Test" Clicked="Button_OnClicked"></Button>

        <ListView x:Name="HistoryItems">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding PostalCode}"></Label>
                            <Label Text="{Binding DateTime}" Grid.Column="1"></Label>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>
</ContentPage>